services:
  hello-service:
    build: ./hello-service
    container_name: hello-service

  gateway:
    build: ./gateway
    container_name: gateway
    ports:
      - "3000:3000"
    depends_on:
      - hello-service
    env_file:
      - .env
